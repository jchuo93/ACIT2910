[Fixed]Added a meta tag that was preventing the navbar to collapse in chrome developer mobile test mode
[Fixed] Added image responsiveness classes to ajax responses for food pages
[Fixed]When the input field for the spinner is empty, added an if statement to give a more informative response rather than "$NaN" (all food pages)
[Fixed] Changed spinner update to work with on('change') instead of click for mobile convenience. (all food pages)
